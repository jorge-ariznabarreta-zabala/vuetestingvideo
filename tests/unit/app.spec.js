import { mount } from "@vue/test-utils";
import App  from "@/App.vue";
//  describe('test unitario de App', ()=>{
//      it('contiene la palabra Contador', () => {
//          const wrapper = mount(App);
       
//          expect(mount(App).html()).toContain("Contador")
//      });
//      it('Mandar count que es 1', ()=>{
//          const wrapper = mount(App, {props: {count: 1,}});
//         ;
//          expect(wrapper.html()).toContain("<h1>Contador: 1</h1>")
//      });
//  });
test("noMolestes", ()=>{});
